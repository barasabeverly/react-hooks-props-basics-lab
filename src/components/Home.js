import React from "react";

function Home(name, city) {
  return (
    <div id="home">
      <h1 style={{ color: "firebrick" }}>
        {Barasa.name} is a Web Developer from {props.city}
      </h1>
    </div>
  );
}

export default Home;
